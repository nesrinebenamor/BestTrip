imports:
    - { resource: parameters.yml }
    - { resource: security.yml }
    - { resource: @MyAppUserBundle/Resources/config/config.yml }

parameters:
    myapp_user.emails.contact_email: besttrippidev@gmail.com

framework:
    #esi:             ~
    translator:      { fallback: "%locale%" }
    secret:          "%secret%"
    router:
        resource: "%kernel.root_dir%/config/routing.yml"
        strict_requirements: ~
    form:            ~
    csrf_protection: ~
    validation:      { enable_annotations: true }
    templating:
        engines: ['twig']
        #assets_version: SomeVersionScheme
    default_locale:  "%locale%"
    trusted_hosts:   ~
    trusted_proxies: ~
    session:
        # handler_id set to null will use default session handler from php.ini
        handler_id:  ~
    fragments:       ~
    http_method_override: true

# Twig Configuration
twig:
    debug:            "%kernel.debug%"
    strict_variables: "%kernel.debug%"

# Assetic Configuration
assetic:
    debug:          "%kernel.debug%"
    use_controller: false
    bundles:        [ ]
    #java: /usr/bin/java
    filters:
        cssrewrite: ~
        #closure:
        #    jar: "%kernel.root_dir%/Resources/java/compiler.jar"
        #yui_css:
        #    jar: "%kernel.root_dir%/Resources/java/yuicompressor-2.4.7.jar"

# Doctrine Configuration
doctrine:
    dbal:
        driver:   "%database_driver%"
        host:     "%database_host%"
        port:     "%database_port%"
        dbname:   "%database_name%"
        user:     "%database_user%"
        password: "%database_password%"
        charset:  UTF8
        # if using pdo_sqlite as your database driver:
        #   1. add the path in parameters.yml
        #     e.g. database_path: "%kernel.root_dir%/data/data.db3"
        #   2. Uncomment database_path in parameters.yml.dist
        #   3. Uncomment next line:
        #     path:     "%database_path%"

    orm:
        auto_generate_proxy_classes: "%kernel.debug%"
        auto_mapping: true

# Swiftmailer Configuration
swiftmailer:
    transport: "%mailer_transport%"
    encryption: ssl
    auth_mode: login
    host: smtp.gmail.com   
    username: besttrippidev@gmail.com
    password: esprittd123456789
    spool: { type: memory }
    disable_delivery: false
    
fos_user: 
     db_driver: orm # other valid values are 'mongodb', 'couchdb' and 'propel' 
     firewall_name: main 
     user_class: MyApp\UserBundle\Entity\Utilisateur
     from_email:
    # Adresse de l'expediteur
        #address: besttrippidev@gmail.com
    # Nom de l'expediteur
        sender_name: Admin de BestTrip.com
     profile:
        form:
            type: myapp_user_profile
            #name: myapp_user_profile_form
            #validation_groups: [Profile, Default]
     change_password:
        form:
            type: fos_user_change_password
            name: fos_user_change_password_form
            validation_groups: [ChangePassword, Default]
     # Permet de renseigner le nouveau formulaire d'inscription    
     registration:
        confirmation:
            from_email: # Use this node only if you don't want the global email address for the confirmation email
                address:       besttrippidev@gmail.com
                sender_name:   Admin de BestTrip.com
            enabled: true
            template: MyAppUserBundle:Registration:email.txt.twig
     
        form:
            type: myapp_user_registration

nomaya_social:
    buttons:
        facebook:
            url:       "www.facebook.com/Esprit"
            locale:    "fr_FR"
            send:      false
            width:     300
            showFaces: false
            layout:    "button_count"
        twitter:
            url:       null
            locale:    "fr"
            message:   "je veux partager ça avec vous"
            text:      "tweet"
            via:       "Esprit Test"
            tag:       "Esprit"
        googleplus:
            url:       null
            locale:    "fr"
            size:      medium
            annotation:  "bubble"
            width:     300
        linkedin:
            url:       null
            locale:    "fr_FR"
            counter:    "right"
        pinterest:
            url:       null
    theme:             'default'  # optional       
  
hwi_oauth:
    connect:
        account_connector: my_user_provider
    # name of the firewall in which this bundle is active, this setting MUST be set
    firewall_name: main
    fosub:
        username_iterations: 30
        properties:
            # these properties will be used/redefined later in the custom FOSUBUserProvider service.
            facebook: facebook_id
    resource_owners:
        facebook:
            type:                facebook
            client_id:           672757779502648
            client_secret:       502c5f639dac0c457c94acccd1308855
            scope:               "email"        
#  firewall_name: secure_area
#  #connect: 
#      #confirmation: true
#  resource_owners:
#    facebook:
#      type: facebook
#      client_id: 672757779502648
#      client_secret: 502c5f639dac0c457c94acccd1308855   
#      scope:   "email"
      
#services:
#    hwi_oauth.user.provider.entity:
#        class: HWI\Bundle\OAuthBundle\Security\Core\User\OAuthUserProvider      