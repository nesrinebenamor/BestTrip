{% extends 'MyApp\UserBundle::layoutBack.html.twig' %}
{% block body -%}
       {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
    <h1>Que voulez vous recommander?</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('recommandation') }}">
            Retour à la liste de toutes les recommandations
        </a>
    </li>
</ul>
{% else %}
        <meta http-equiv="refresh" content="0; url=http://localhost/ProjetBestTrip/web/app_dev.php/login" />
{% endif %}
{% endblock %}
